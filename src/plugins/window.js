// import something here
import Window from 'components/system/Window.vue';

// leave the export, even if you don't use it
export default ({ app, router, Vue }) => {

	Vue.component('nifrat-window',Window);
  // something to do
}
